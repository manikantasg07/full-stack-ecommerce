const db = require("../models/index");
const z = require("zod");

const User = z.object({
    firstName : z.string(),
    lastName : z.string(),
})


const register = (req,res)=>{

    try {
       
    } catch (error) {
        
    }

}